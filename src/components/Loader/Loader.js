// ** React Imports **
import React from "react"; // Import the React library, which is necessary for creating React components and using JSX syntax in this file

const Loader = () => {
  return <span className="loader"></span>;
};

export default Loader;
